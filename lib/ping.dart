import 'package:flutter/material.dart';

class PingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Ping juga belum pak"),
      ),
    );
  }
}
