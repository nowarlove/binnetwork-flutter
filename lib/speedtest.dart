import 'package:flutter/material.dart';

class SpeedtestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Speedtest juga pak"),
      ),
    );
  }
}
